# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: F:/wine_box_seg/data/aug_data # 数据集根目录
train: box_code_train/images # 训练集图像路径
val: box_code_val/images # 验证集图像路径
test: # 测试集图像路径（可有可无）

# Classes
names:
  0: box_code_out
  1: box_code_in

